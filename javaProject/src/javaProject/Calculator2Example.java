package javaProject;

public class Calculator2Example {
	public static void main(String[] args) {

		Calculator2 myCalc = new Calculator2(); // Calculator2 클래스(라이브러리 클래스)의 기본 생성자 호출

		myCalc.execute(); // Calculator2 클래스(라이브러리 클래스) 내부 execute() 메소드 호출 // 반환된 문자열 출력
	}
}
